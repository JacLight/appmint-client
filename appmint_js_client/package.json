{
  "name": "@appmint/js-client",
  "version": "0.1.0",
  "description": "Appmint AppEngine client for TypeScript — app authentication, sessions and requests, with no framework.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "files": ["dist", "README.md"],
  "scripts": { "build": "tsc -p tsconfig.json", "prepublishOnly": "npm run build" },
  "keywords": ["appmint", "appengine", "api", "client", "typescript"],
  "devDependencies": { "typescript": "^5.6.0" }
}
